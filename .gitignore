# dependencies
node_modules/

# build output
dist/
.astro/
.vercel/

# env / secrets
.env
.env.*
!.env.example

# Derived from the design system (the source of truth). Regenerated by `npm run sync:ds`.
src/ds/
public/ds/

# os
.DS_Store
Thumbs.db

# dev screenshots
.shots/
hero-candidates/

# Bizalmas referencia-forrás PDF-ek (ügyfél-PII-t tartalmaznak — ne kerüljön gitbe).
# A PII-mentes kivonat: project docs/referenciák/referenciafal_adatok_DRAFT.md + src/lib/references.ts
project docs/referenciák/*.pdf
