# Input

Labelled text input, 8px radius, green focus ring. Optional leading icon, hint and error states.

```jsx
<Input label="Név" placeholder="Az Ön neve" />
<Input label="E-mail" icon={<Mail/>} type="email" hint="Munkahelyi cím" />
<Input label="Telefon" error="Kötelező mező" />
```
