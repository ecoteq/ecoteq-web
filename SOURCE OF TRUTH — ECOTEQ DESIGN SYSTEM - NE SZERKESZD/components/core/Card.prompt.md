# Card

Generic white surface with a soft navy-tinted shadow and hairline border. Set `interactive` for clickable cards (hover lift).

```jsx
<Card padding={32}>…</Card>
<Card interactive onClick={…}>…</Card>
```
