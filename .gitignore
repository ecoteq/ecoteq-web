# dependencies
node_modules/

# build output
dist/
.astro/
.vercel/

# env / secrets
.env
.env.*
!.env.example

# Derived from the design system (the source of truth). Regenerated by `npm run sync:ds`.
src/ds/
public/ds/

# os
.DS_Store
Thumbs.db

# dev screenshots
.shots/
hero-candidates/

# Bizalmas referencia-forrás PDF-ek + nyers ügyfél-fotók/videók (PII — ne kerüljön gitbe).
# A PII-mentes kivonat: project docs/referenciák/referenciafal_adatok_DRAFT.md + src/lib/references.ts
# A weboldalra a feldolgozott (vágott, anonimizált, webp) változatok kerülnek a public/ alá.
project docs/referenciák/*
!project docs/referenciák/*.md

# Nyers ügyféllogó-forrás (több nem használt + nevesíthetetlen, pl. DIPA — de-anonimizálás
# elkerülése). A weboldali, feldolgozott badge-ek a public/referenciak/logok/ alatt vannak.
project docs/logo/
